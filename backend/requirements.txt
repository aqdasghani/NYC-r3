# GreenShop AI Backend — pinned dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32        # required for multipart UploadFile (scan-invoice)
apscheduler==3.11.0             # background detection job
PyJWT==2.10.1                   # JWT auth (import name: jwt)
bcrypt==4.2.1                   # password hashing (used directly, no passlib)
httpx==0.28.1                   # OpenAI / Gemini / Google Vision HTTP calls
pillow==12.2.0                  # image preprocessing for OCR
pytesseract==0.3.13             # local OCR fallback (needs OS tesseract binary)
openai==2.50.0                  # LLM recommendations (optional)
python-dotenv==1.0.1
pandas>=2.0.0
numpy>=1.26.0
websockets>=12.0
pytest==8.4.2
pytest-asyncio==0.23.5
